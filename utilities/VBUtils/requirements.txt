# Requirements for VB Complete Importer
# Core dependencies (required)
Pillow>=8.0.0

# OCR dependencies (optional - for automatic character name extraction)
# Install these if you want automatic character name extraction from sprites:
# pip install opencv-python numpy pytesseract

# opencv-python>=4.5.0
# numpy>=1.20.0
# pytesseract>=0.3.8

# Note: For pytesseract to work, you also need to install Tesseract OCR:
# Windows: Download and install from https://github.com/UB-Mannheim/tesseract/wiki
# Ubuntu: sudo apt install tesseract-ocr
# macOS: brew install tesseract